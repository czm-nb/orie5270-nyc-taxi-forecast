# Analysis

Use this folder for **exploratory work** (notebooks, one-off scripts, figures) if you extend the project. The **main, tested pipeline** lives in `src/nyc_taxi_forecast/` (`build-hourly`, `train-baseline`, web app).
